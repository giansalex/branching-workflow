{
  "name": "branching-workflow",
  "version": "1.0.0",
  "description": "Keep your Branching Model from Pull Request.",
  "author": "Giancarlos Salas <giansalex@gmail.com> (https://github.com/giansalex/branching-workflow)",
  "license": "ISC",
  "repository": "https://github.com/giansalex/branching-workflow.git",
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest --coverage && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "^6.2.0"
  },
  "devDependencies": {
    "jest": "^22.4.4",
    "nodemon": "^1.17.5",
    "smee-client": "^1.0.1",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
